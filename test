#!/usr/bin/python3

import subprocess 

def tcaddpoints(steamid, points, inst):
    subprocess.run("""arkmanager rconcmd 'ScriptCommand TCsAR AddARcTotal "%s" %s' @%s""" % (steamid, points, inst), shell=True)
